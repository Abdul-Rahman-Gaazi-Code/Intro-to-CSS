Question 1: Orange, inline CSS always wins over external CSS no matter how you style it unless you use the important tag.

Question 2:The only way to force green in style.css will be to use !important:, !important overrides even inline CSS.

Question 3: Just changing the color from red to blue will not work, because the inline CSS still wins but adding the !important tag it is possible. Yes it is possible but only with !important.